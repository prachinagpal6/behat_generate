@api @javascript @{{ node_type }}
Feature: Creation of {{ node_type }} content type
  To verify {{ node_type }} content type should have correct field type,
  and can be created by User.

  Scenario: Generate {{ node_type }} content nodes.
    Given I am logged in as a user with the "create {{ node_type|raw }} content" permission
    When I am on "node/add/{{ node_type|raw }}"
    Then I fill in "edit-title" with "Test {{ node_type|raw }} Node"
{{ test|raw }}
    #Now Pressing Submit
    When I press the "Save and publish" button
    Then I should see "{{ label|raw }} Test {{ node_type|raw }} has been created."
